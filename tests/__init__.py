"""
tests/__init__.py

Назначение:
- маркирует директорию `tests/` как валидный Python‑пакет;
- обеспечивает корректное обнаружение тестов инструментом pytest.

Состав пакета:
- `conftest.py`: содержит фикстуры для тестирования, например
  `client` на базе `TestClient` из FastAPI;
- `test_endpoints.py`: включает класс `TestEndpoints` с тестовыми
  сценариями для проверки HTTP‑эндпоинтов приложения (например, тест
  корневого эндпоинта `/`).
"""
